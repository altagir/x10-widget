# Automatically added by dh_apparmor
if [ "$1" = "purge" ] && ! [ -e "/etc/apparmor.d/usr.lib.kde4.libexec.x10_service" ] ; then
    rm -f "/etc/apparmor.d/disable/usr.lib.kde4.libexec.x10_service" || true
    rm -f "/etc/apparmor.d/force-complain/usr.lib.kde4.libexec.x10_service" || true
    rm -f "/etc/apparmor.d/local/usr.lib.kde4.libexec.x10_service" || true
    rmdir /etc/apparmor.d/local 2>/dev/null || true
fi
# End automatically added section
